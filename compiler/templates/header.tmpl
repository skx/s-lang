        # Define our entry-point
        .global _start

        #
        # Code
        #
        .section .text
_start:

        #
        # Setup our initial heap allocator
        #
        call init_heap